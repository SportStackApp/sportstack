# .github/workflows/scrape-sunraysia.yml
#
# Match Scraper — Sunraysia Hockey Association
# Scrapes match results and player appearances from RevSports.
# Writes directly to Supabase (revsports_players table).
# Also saves a CSV + JSON backup to the /data folder in the repo.
#
# Sunraysia runs both grass field and indoor competitions.
# The scraper auto-detects all grades from the games page — no config needed.
#
# Schedule (AEST = UTC+10):
#   Weeknights (Mon–Fri) — once at 2am AEST (16:00 UTC previous day)
#   Weekends (Sat–Sun)   — every 30 minutes from 8:00am to 8:00pm AEST
#
# Note: Sunraysia runs at :30 past the hour on weekends. HB runs at :00 — so they don't clash.
#
# Manual trigger: Actions tab → "Scrape — Sunraysia Hockey Association" → Run workflow

name: Scrape — Sunraysia Hockey Association

on:
  # ── Scheduled runs ──────────────────────────────────────────
  schedule:
    # Weeknights (Mon–Fri) at 2am AEST (16:00 UTC previous day)
    - cron: "0 16 * * 0-4"   # 2am Tue–Sat AEST = 16:00 UTC Mon–Fri

    # Weekends (Sat–Sun) — every 30 min from 8am to 8pm AEST
    # Staggered 30 min after HB to avoid running at exactly the same time
    # 8:30am AEST = 22:30 UTC (previous day)
    - cron: "30 22 * * 5,6"  # 8:30am
    - cron: "0 23 * * 5,6"   # 9:00am  — note: alternates with HB at :00/:30
    - cron: "30 23 * * 5,6"  # 9:30am
    - cron: "0 0  * * 6,0"   # 10:00am
    - cron: "30 0  * * 6,0"  # 10:30am
    - cron: "0 1  * * 6,0"   # 11:00am
    - cron: "30 1  * * 6,0"  # 11:30am
    - cron: "0 2  * * 6,0"   # 12:00pm
    - cron: "30 2  * * 6,0"  # 12:30pm
    - cron: "0 3  * * 6,0"   # 1:00pm
    - cron: "30 3  * * 6,0"  # 1:30pm
    - cron: "0 4  * * 6,0"   # 2:00pm
    - cron: "30 4  * * 6,0"  # 2:30pm
    - cron: "0 5  * * 6,0"   # 3:00pm
    - cron: "30 5  * * 6,0"  # 3:30pm
    - cron: "0 6  * * 6,0"   # 4:00pm
    - cron: "30 6  * * 6,0"  # 4:30pm
    - cron: "0 7  * * 6,0"   # 5:00pm
    - cron: "30 7  * * 6,0"  # 5:30pm
    - cron: "0 8  * * 6,0"   # 6:00pm
    - cron: "30 8  * * 6,0"  # 6:30pm
    - cron: "0 9  * * 6,0"   # 7:00pm
    - cron: "30 9  * * 6,0"  # 7:30pm
    - cron: "0 10 * * 6,0"   # 8:00pm

  # ── Manual trigger ───────────────────────────────────────────
  workflow_dispatch:
    inputs:
      only_grades:
        description: 'Grades to scrape (comma-separated, blank = all)'
        required: false
        default: ''
      only_rounds:
        description: 'Rounds to scrape (comma-separated, blank = all)'
        required: false
        default: ''
      only_team:
        description: 'Team name filter (partial match, blank = all)'
        required: false
        default: ''

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1. Check out the repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3. Install dependencies
      #    requests + beautifulsoup4 for scraping
      #    supabase for writing results to the database
      - name: Install dependencies
        run: pip install requests beautifulsoup4 supabase

      # 4. Run the match scraper for Sunraysia
      #    SUPABASE_URL and SUPABASE_SERVICE_KEY come from repo Secrets
      #    PORTAL_URL points to the Sunraysia RevSports site
      - name: Run scraper
        env:
          PORTAL_URL:           "https://www.sunraysiahockey.com.au"
          ASSOCIATION_NAME:     "Sunraysia Hockey Association"
          OUTPUT_DIR:           "data/sunraysia"
          SUPABASE_URL:         ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          ONLY_GRADES:          ${{ github.event.inputs.only_grades || '' }}
          ONLY_ROUNDS:          ${{ github.event.inputs.only_rounds || '' }}
          ONLY_TEAM:            ${{ github.event.inputs.only_team   || '' }}
        run: python scraper/scraper.py

      # 5. Commit the CSV/JSON backup back into the repo
      - name: Commit results
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "scrape(sunraysia): $(date '+%Y-%m-%d %H:%M') AEST"
          git pull --rebase -X theirs
          git push
