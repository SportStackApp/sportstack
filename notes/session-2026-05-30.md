# Session Handover — 2026-05-30

## What we did this session

### 1. RevSports Mappings Page — upgraded to 7 tabs
- Replaced the old single-tab `RevSportsTeamMappings.tsx` with a new `RevSportsMappings.tsx`
- New tab order: **Teams | Grades | Clubs | Venues | Pitches | Players | Umpires**
- Each tab uses a 4-column layout: CSV Column | Scraped Value | Maps To | Supabase Destination
- Added a reusable `multi-select.tsx` component for filter bars
- Routing updated in `App.tsx` and `AppLayout.tsx`

### 2. New Supabase mapping tables created
All created via `apply_migration` on project `svierarfcolhcfjpmwck`:
- `revsports_grade_mappings` — revsports_grade → divisions.id
- `revsports_club_mappings` — revsports_club_name → clubs.id
- `revsports_pitch_mappings` — revsports_venue_name + revsports_pitch_name → pitches.id
- `revsports_umpire_mappings` — revsports_umpire_name → profiles.id
- `revsports_player_mappings` — already existed, confirmed correct
- `revsports_fixture_mappings` — already existed, confirmed correct
- `revsports_team_mappings` — updated to add `club_name` and `division_name` columns

### 3. revsports_players table updates
Added two new columns:
- `pitch` (text) — stores pitch name separately from venue
- `association` (text) — stamps every row with which association it came from

### 4. Scraper updated (scraper/scraper.py)
- Added `ASSOCIATION_NAME` config variable (reads from env var, defaults to "Hockey Ballarat")
- Stamps every CSV row with `association` as the first column
- Output dir now reads from `OUTPUT_DIR` env var (defaults to `../data`)

### 5. GitHub Actions — two workflow files
- Renamed `scrape.yml` → `scrape-hockey-ballarat.yml`
- Created `scrape-sunraysia.yml`
- Both use the same `scraper.py` with different env vars:
  - Hockey Ballarat: PORTAL_URL=revolutionise.com.au/hockeyballarat, OUTPUT_DIR=data/hockey-ballarat
  - Sunraysia: PORTAL_URL=sunraysiahockey.com.au, OUTPUT_DIR=data/sunraysia
- Added `git pull --rebase` before `git push` to prevent push conflicts
- Both scrapers ran successfully today and committed data to GitHub

### 6. Column mapping analysis completed
Went through every CSV column and determined:

| Column | Action |
|---|---|
| association | Direct write to revsports_players.association |
| grade | Needs Grades mapping tab → divisions.id |
| round | Direct write — no mapping needed |
| game_date | Direct write — combine with game_time |
| game_time | Direct write — combine with game_date |
| venue | Needs Venues mapping tab → venues.id |
| pitch | Needs Pitches mapping tab (venue+pitch combo) → pitches.id |
| home_team | Needs Teams mapping tab → teams.id |
| away_team | Needs Teams mapping tab → teams.id |
| club_name | Needs Clubs mapping tab → clubs.id |
| team | Needs Teams mapping tab (club+grade+team context) → teams.id |
| player_name | Needs Players mapping tab (club+grade+team+jersey context) → profiles.id |
| jersey | Direct write |
| role | Ignore for now (mostly empty) |
| attended | Direct write |
| goals | Direct write |
| green_cards | Direct write |
| yellow_cards | Direct write |
| red_cards | Direct write |
| match_url | Direct write — also used as unique game key |
| umpire_1 | Needs Umpires mapping tab → profiles.id |
| umpire_2 | Needs Umpires mapping tab → profiles.id |
| scraped_at | Direct write |

---

## What's still outstanding

### High priority
- [ ] **Grades tab** — needs association context in dropdown (`[Association] — [Division]`)
- [ ] **Upload scraper data to Supabase automatically** — currently scraper only writes CSV to GitHub, not to Supabase. Need to add `supabase-py` to the scraper and upsert rows directly after each run
- [ ] **Import full CSV into revsports_players** — current Supabase data only has 4 grades (963 rows). Need to import the full Hockey Ballarat and Sunraysia CSVs
- [ ] **RLS policies** on all new mapping tables — none have been configured yet

### Medium priority
- [ ] Clean up old `data/hockey_results.csv` and `data/hockey_results.json` files in the repo root
- [ ] **MVP Voting** — RLS policies not yet configured on voting tables
- [ ] **MVP Voting** — player dropdown needs filtering to voter's own team (alphabetical)
- [ ] **Fixture matching logic** — use grade + game_date + mapped home_team_id + away_team_id as 4-point match

### Lower priority
- [ ] Commit all Antigravity front-end changes to `dev` branch (currently on main)
- [ ] Add team filter dropdown to Fixtures Management page
- [ ] Address duplicate team names (see notes/known-issues.md)

---

## Key decisions made this session

- **No Fixtures mapping tab** — fixtures can be derived from grade + date + home/away team mapping
- **Fixture unique key** = grade + game_date + mapped home_team_id + mapped away_team_id
- **Teams mapping** keyed on club_name + grade + team (3-part key to avoid duplicates)
- **Pitches mapping** keyed on venue_name + pitch_name (venue is inherited from pitch record)
- **One scraper file, two GitHub Actions** — env vars control which association is scraped
- **Separate output subfolders** — data/hockey-ballarat and data/sunraysia

---

## Active Supabase project
`svierarfcolhcfjpmwck` (SportStackApp) — only active project

## GitHub
`github.com/SportStackApp/sportstack` — active branch: `main`
Vercel auto-deploys on push to main.

---

## Evening session � 2026-05-30 (continuation)

### 7. Scraper � is_fillin and is_removed columns added
- Added is_fillin (boolean) and is_removed (boolean) columns to evsports_players table in Supabase
- Updated scraper/scraper.py to detect Fill-ins and Removed from team sections in match card tables
- Both fields default to False for regular squad players
- ttended logic updated: removed players always False, fill-ins always True

### 8. Supabase upsert added to scraper
- Installed supabase==2.10.0 Python package locally
- Added upsert block to end of main() in scraper.py
- Reads SUPABASE_URL and SUPABASE_SERVICE_KEY from environment variables
- Upserts in batches of 200 rows to avoid timeouts
- Unique conflict key: match_url + player_name + team + is_fillin
  - Reason: two players with same name (e.g. Jason H.) can appear in same game if one is a regular and one is a fill-in
- If keys not set, upsert is skipped gracefully with a warning

### 9. Supabase unique constraint added
- Added constraint evsports_players_match_player_team_fillin_unique on (match_url, player_name, team, is_fillin)
- Removed duplicate rows from old imports before adding constraint
- Old rows with null association cleaned up

### 10. GitHub Actions secrets added
- SUPABASE_URL added to repo secrets
- SUPABASE_SERVICE_KEY added to repo secrets

### 11. GitHub Actions workflows updated
- Both scrape-hockey-ballarat.yml and scrape-sunraysia.yml updated:
  - Added supabase==2.10.0 to pip install step
  - Added SUPABASE_URL and SUPABASE_SERVICE_KEY env vars to Run scraper step
  - Fixed git conflict handling: git pull --rebase -X theirs (prevents failures when both scrapers run simultaneously)

### 12. Supabase data as of end of session
| Association | Grade | Rows |
|---|---|---|
| Hockey Ballarat | Division 1 Men | 338 |
| Hockey Ballarat | Division 2 Men | 582 |
| Hockey Ballarat | Under 11 Open | 345 |
| Hockey Ballarat | Under 14 Open | 244 |
| Hockey Ballarat | Under 16 Open | 11 ?? low � investigate |
| Hockey Ballarat | Womens | 487 |
| Sunraysia | Indoor Mixed | 298 |
| Sunraysia | Men | 288 |
| Sunraysia | U11 Mixed | 156 |
| Sunraysia | U13 Boys | 247 |
| Sunraysia | U13 Girls | 270 |
| Sunraysia | U16 Mixed | 261 |
| Sunraysia | Women | 300 |
| null | Various | 232 ?? old rows � need cleanup |

---

## Updated outstanding tasks

### High priority
- [ ] Clean up 232 old rows with null association in revsports_players
- [ ] Investigate Under 16 Open only having 11 rows (should be more)
- [ ] Re-run Sunraysia workflow after workflow fix is pushed (last run failed on git conflict)
- [ ] Grades tab � needs association context in dropdown
- [ ] RLS policies on all mapping tables

### Medium priority
- [ ] MVP Voting � RLS policies + player dropdown filter
- [ ] Fixture matching logic

### Lower priority
- [ ] Commit front-end changes to dev branch
- [ ] Team filter on Fixtures Management page
- [ ] Duplicate team names decision
